# Google Maps JavaScript API key, used by the live map on the dashboard
# (src/components/GoogleMap.tsx). Without it the map falls back to a static
# view — everything else in the demo still works.
VITE_GOOGLE_MAPS_API_KEY=
